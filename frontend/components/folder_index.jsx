const React = require('react');

const FolderIndex = React.createClass({
  render() {
    return (
      <section>
        <p>Folders go here</p>
      </section>
    );
  }
});

module.exports = FolderIndex;